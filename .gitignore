# Node.js dependencies and build artifacts
node_modules/
node-bot/dist/
node-bot/*.tsbuildinfo
node-bot/.env
node-bot/.env.local
node-bot/.env.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
logs/
python/logs/
var/logs/

# Python artifacts and virtual environments
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.pytest_cache/
.venv/
venv/
python/.cache/
python/orchestrator/.cache/
python/.openai/
python/.secrets/
python/.tokens/
/.python_history
python/action_backlog*.json
# LangGraph 実行時のデバッグアーティファクト（秘匿情報が混入しうるため除外）
.langgraph/
python/.langgraph/
# Mermaid 図式の一時出力（内部フローや座標が含まれる可能性があるため除外）
python/mermaid_diagrams/
# 建築ジョブなどで生成されるチェックポイントファイル（進捗や位置情報を含むため秘匿対象）
checkpoints/
python/checkpoints/
# Python agent のローカル設定ファイル（秘匿情報を含む可能性があるため除外）
python/config.local.py
python/config.local.json

# OpenAI SDK が生成する可能性のある API 呼び出しログ（秘匿情報混入リスク）
openai.log
python/openai.log
# Responses API のデバッグ時に生成されるキャッシュファイル（API 応答に秘匿情報が含まれる場合がある）
python/responses_cache.json

# Cursor エディタのセッション情報（秘匿情報が混入する恐れがあるため除外）
!.cursor/rules/
python/.cursor/

# Local runtime artifacts created while 検証・デバッグした際に生成される履歴ファイル
python/.python_history

# Environment and secret files
.env
.env.local
.env.development
.env.production
.env.staging
.env.test
.envrc
.npmrc
.yarnrc
.pypirc
.python-version
python/.env
python/.env.*
python/.envrc
# runtime 配下で個別検証する際のローカル秘密設定を誤コミットしないための除外ルール
python/runtime/.env*
python/runtime/.secrets/
*.pem
*.key
*.p12
# runtime/services 配下に個別検証用のローカル設定を置く際に秘匿情報を含めないように除外
node-bot/runtime/services/*.local.*
# OpenTelemetry コレクターの設定ファイル（エンドポイントや認証情報が含まれる可能性があるため除外）
otel-collector*.yaml
otel-collector*.yml
# ローカルでエクスポートしたトレース/メトリクスのダンプ（API キーやチャット内容が含まれる可能性があるため除外）
var/telemetry/
# Local Paper server binaries（バージョン固定の jar を誤コミットしないための安全策）
paper.jar
paper-*.jar
bridge-plugin/.gradle/
bridge-plugin/build/
bridge-plugin/libs/*.jar
# Paper 実行環境で生成される AgentBridge 設定（API キーを含むため除外）
plugins/AgentBridge/
# AgentBridge のローカル上書き設定（LangGraph エンドポイントやシークレットを含むため除外）
bridge-plugin/config.local.yml
bridge-plugin/src/main/resources/config.local.yml

# Editor and OS artifacts
.DS_Store
Thumbs.db
.idea/
.vscode/

# Vitest cache / coverage
node-bot/.vitest/
node-bot/coverage/
# Skill library runtime storage（学習履歴やプレイヤー座標を含むため除外）
var/skills/
node-bot/var/skills/
node-bot/var/
python/skills/*.runtime.json
# Skill コマンド履歴ログ（操作内容や座標などの秘匿情報が含まれる可能性があるため除外）
node-bot/runtime/commands/skill_history*.log
# MineDojo 連携で使用するキャッシュ/データセット（API 応答やデモ軌跡に秘匿情報が含まれる）
var/cache/minedojo/
var/datasets/minedojo/
python/minedojo_cache/
# Reflexion ログの永続化ファイル（失敗手順や復旧履歴に敏感情報が含まれるため除外）
var/memory/
# VPT 推論用のチェックポイントや推論キャッシュ（再配布禁止ライセンスに抵触する恐れがあるため除外）
var/vpt/
python/vpt_cache/
python/services/vpt_checkpoints/
# LangSmith SDK が生成するローカルキャッシュやトレースダンプ（API キーやチャット内容が含まれうるため除外）
.langsmith/
python/.langsmith/
langsmith.db
